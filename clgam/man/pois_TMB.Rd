% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pois_TMB.R
\name{pois_TMB}
\alias{pois_TMB}
\title{Poisson CL-GAMM via TMB Laplace approximation (\code{pois_TMB})}
\usage{
pois_TMB(
  y,
  x1,
  x2,
  efine = NULL,
  lcovfine = NULL,
  nlcovfine = NULL,
  C,
  x1lim = NULL,
  x2lim = NULL,
  ndx = c(15, 15),
  bdeg = c(3, 3),
  pord = c(2, 2),
  decom = 1,
  parold = c(1, 1),
  paroldnl = NULL,
  ndxnl = 15,
  bdegnl = 3,
  pordnl = 2,
  nl.basis = c("pspline", "legacy"),
  orth.smooth = NULL,
  sparse.backend = "auto",
  silent = TRUE,
  control = list(iter.max = 200, eval.max = 400)
)
}
\arguments{
\item{y}{coarse counts (length \eqn{n})}

\item{x1, x2}{fine-scale spatial coordinates (length \eqn{m})}

\item{efine}{fine-scale exposure / expected counts (length \eqn{m}; default 1)}

\item{lcovfine}{optional fine-scale linear covariates (\(m \times p\))}

\item{nlcovfine}{optional fine-scale covariates for univariate P-spline
smooths (\(m \times K\))}

\item{C}{composition matrix (\(n \times m\))}

\item{x1lim, x2lim}{optional coordinate limits}

\item{ndx, bdeg, pord}{spatial P-spline settings}

\item{decom}{mixed-model decomposition for spatial bases}

\item{parold, paroldnl}{initial variance components}

\item{ndxnl, bdegnl, pordnl}{settings for each smooth covariate}

\item{nl.basis}{how smooth covariates enter the mixed model:
\code{"pspline"} uses the P-spline null space \code{Xxk} plus penalized
\code{Zxk} from \code{mm_basis}; \code{"legacy"} (default here) matches
archival SMiMR code (raw \code{nlcovfine} unpenalized in \code{X} +
\code{Zxk}). Prefer \code{\link{clgam}}, which defaults to
\code{nl.basis="pspline"}.}

\item{orth.smooth}{if \code{TRUE}, project spatial random bases orthogonal
to the additive smooth span so \eqn{f(s)} cannot absorb \eqn{g(z)}. Defaults to
\code{TRUE} for \code{nl.basis="pspline"}, \code{FALSE} for
\code{"legacy"}.}

\item{sparse.backend}{composition-matrix backend (see \code{\link{as_comp_C}})}

\item{silent}{suppress TMB / optimizer chatter}

\item{control}{passed to \code{\link[stats]{nlminb}}}
}
\value{
An object of class \code{"clgam"} with \code{family="spatial_tmb"},
  including \code{eta}, \code{sd.eta} (from \code{sdreport} / ADREPORT),
  \code{var.comp} (\(\tau^2\)), and \code{tmb} fit bits.
}
\description{
Same mixed-model design as \code{\link{pois_SOP}} (anisotropic spatial
P-splines + optional fine-scale univariate smooths), but integrates the
random effects with a Laplace approximation to the marginal Poisson
composite-link likelihood via \pkg{TMB}. Intended as a robustness check
against PIRLS+SOP (PQL-style) for simulations and coverage.
}
\details{
Requires \pkg{TMB} (Suggests). The template lives in \code{inst/TMB/} and is
compiled at runtime (separate DLL from the Rcpp SOP kernels).
}
