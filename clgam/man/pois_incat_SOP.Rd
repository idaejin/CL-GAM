% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pois_incat_SOP.R
\name{pois_incat_SOP}
\alias{pois_incat_SOP}
\alias{pois_incat_SAP}
\title{Two-category Poisson CL-GAMM via PIRLS + SOP}
\usage{
pois_incat_SOP(
  y,
  x1,
  x2,
  efine = NULL,
  lcovfine = NULL,
  cat,
  Ccat1,
  Ccat2,
  x1lim = NULL,
  x2lim = NULL,
  ndx = c(15, 15),
  bdeg = c(3, 3),
  pord = c(2, 2),
  thr = c(1e-06, 1e-06),
  maxit = c(100, 100),
  parold = c(1, 1, 1, 1),
  bold = NULL,
  trace = FALSE,
  elements = FALSE,
  decom = 1,
  sparse.backend = "auto",
  family = stats::poisson(),
  structure = c("independent", "contrast")
)

pois_incat_SAP(
  y,
  x1,
  x2,
  efine = NULL,
  lcovfine = NULL,
  cat,
  Ccat1,
  Ccat2,
  x1lim = NULL,
  x2lim = NULL,
  ndx = c(15, 15),
  bdeg = c(3, 3),
  pord = c(2, 2),
  thr = c(1e-06, 1e-06),
  maxit = c(100, 100),
  parold = c(1, 1, 1, 1),
  bold = NULL,
  trace = FALSE,
  elements = FALSE,
  decom = 1,
  sparse.backend = "auto",
  family = stats::poisson(),
  structure = c("independent", "contrast")
)
}
\arguments{
\item{y}{coarse counts for both groups (stacked)}

\item{x1, x2}{fine coordinates (stacked groups)}

\item{efine}{fine exposures}

\item{lcovfine}{optional linear covariates}

\item{cat}{grouping factor of length \eqn{m}}

\item{Ccat1, Ccat2}{composition matrices per group}

\item{x1lim, x2lim}{optional coordinate limits}

\item{ndx, bdeg, pord}{spatial P-spline settings}

\item{thr, maxit, parold, bold, trace, elements, decom, sparse.backend}{as in \code{\link{pois_SOP}}}

\item{family}{\code{poisson()} or \code{quasipoisson()} (see
\code{\link{pois_SOP}})}

\item{structure}{\code{"independent"} (default; two separate spatial
fields, manuscript sex contrast) or \code{"contrast"}: shared field
plus difference with the sum-to-zero restriction
\eqn{\eta_1=f+d}, \eqn{\eta_2=f-d} on the smooth spatial deviation.}
}
\value{
A \code{"clgam"} object with stacked \code{eta}, \code{eta.diff},
  \code{eta.shared}, and (if \code{elements=TRUE}) Bayesian SEs
  \code{sd.eta}, \code{sd.dif}, \code{sd.shared}, and the independence
  approximation \code{sd.dif2}. Use \code{\link{clgam_contrast_infer}}
  for pointwise intervals; \code{sd.dif} (not \code{sd.dif2}) is the SE
  for \eqn{\eta_1-\eta_2}.
}
\description{
Prefer \code{\link{clgam_contrast}} in new code.
\code{pois_incat_SAP} is retained as a compatibility alias.
}
\seealso{
\code{\link{clgam_contrast}}, \code{\link{pois_SOP}}
}
